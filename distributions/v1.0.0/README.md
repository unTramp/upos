# U-POS v1.0.0 Portable Distributions

`single-files/` contains the canonical portable transport representation of Modules 01–11 for AI/chat systems that cannot ingest the full repository tree conveniently.

The complete `UPOS_v1_FINAL.zip` release archive is intentionally not committed here. Attach it to the GitHub Release for tag `v1.0.0`.
