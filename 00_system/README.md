# U-POS System Metadata

This directory contains repository-level release metadata and global registries for the frozen U-POS v1 architecture baseline.

These files do not replace module-owned normative artifacts. They index and lock the coordinated release baseline.
