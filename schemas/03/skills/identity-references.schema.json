{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:upos:schema:03:skills:identity-references:0.1.0",
  "title": "UPOS-03 Skills Identity References",
  "description": "Canonical serialization of frozen UPOS-03 Skill identity/reference semantics only.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "skill_ref": { "$ref": "#/$defs/skillRef" },
    "skill_invocation_ref": { "$ref": "#/$defs/skillInvocationRef" },
    "skill_result_ref": { "$ref": "#/$defs/skillResultRef" }
  },
  "$defs": {
    "nonEmptyReference": { "type": "string", "minLength": 1 },
    "skillRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["skill_id", "skill_version"],
      "properties": {
        "skill_id": { "$ref": "#/$defs/nonEmptyReference" },
        "skill_version": { "$ref": "#/$defs/nonEmptyReference" }
      },
      "description": "Versioned Skill identity. skill_id without version is incomplete for a versioned Skill reference."
    },
    "skillInvocationRef": {
      "type": "string",
      "minLength": 1,
      "description": "Reference to one Skill Invocation according to UPOS-03 contracts."
    },
    "skillResultRef": {
      "type": "string",
      "minLength": 1,
      "description": "Serialization field for a Skill Result reference; no new result entity semantics are introduced."
    }
  }
}
